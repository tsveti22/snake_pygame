import pygame

# Initialise pygame
pygame.init()

# Create window
gameDisplay = pygame.display.set_mode((800,600))

# Update (flip) display
pygame.display.update()

# Uninitialise and quit pyga
pygame.quit()
quit()
